# tls-test

ESP32-C6 firmware scaffold for experimenting with a custom TLS + HTTP client stack built on top of ESP-IDF sockets.

## Build and flash

From `embedded/tls-test/`:

```bash
nix-shell
run
```

`run` builds the firmware and flashes it with serial monitor attached.
